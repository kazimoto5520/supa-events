import type { Metadata } from "next";
import "./globals.css";
import { VendorSidebar } from "@/components/dashboard/vendor/VendorSidebar";
import { VendorTopHeader } from "@/components/dashboard/vendor/VendorTopHeader";
import ReactQueryProvider from "@/components/ReactQueryProvider";
import { Toaster } from "@/components/ui/toaster";

export const metadata: Metadata = {
  title: "SupaEvent | Dashboard",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`plus-jakarta-sans-font antialiased`}
      >
        <ReactQueryProvider>
          <div className="flex flex-col h-screen">
            <Toaster />
            <VendorTopHeader />
            <div className="flex flex-1 overflow-hidden">
              <VendorSidebar />
              <main className="flex-1 overflow-y-auto p-6">
                {children}
              </main>
            </div>

          </div>
        </ReactQueryProvider>
      </body>
    </html>
  );
}
